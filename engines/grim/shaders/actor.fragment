#version 150

in vec2 Texcoord;

uniform sampler2D tex;
uniform bool textured;
uniform vec4 color;

out vec4 outColor;

void main()
{
	    outColor = color;
	    if (textured) {
	    	outColor *= texture(tex, Texcoord);
	    }
}
